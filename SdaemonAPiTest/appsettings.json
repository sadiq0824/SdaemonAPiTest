{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Sets the default logging level to Information
      "Microsoft.AspNetCore": "Warning" // Overrides logging for ASP.NET Core framework to Warning
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=DESKTOP-MN8K8AR;Database=SdaemonTest;Trusted_Connection=True;TrustServerCertificate=True;"
    // This is your SQL Server connection string
    // - Trusted_Connection=True: Uses Windows Authentication
    // - TrustServerCertificate=True: Bypasses SSL certificate validation (not recommended for production)
  },
  "AllowedHosts": "*" // Allows all hostnames (useful for development)
}
